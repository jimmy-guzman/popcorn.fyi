{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "passThroughEnv": [
        "DATABASE_URL",
        "DATABASE_URL_UNPOOLED",
        "COMPATIBILITY_DATE",
        "TMDB_API_TOKEN",
        "TMDB_API_KEY"
      ],
      "outputs": [".vercel/**", ".vinxi/**", ".output/**"]
    }
  }
}
