{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "passThroughEnv": ["TMDB_API_TOKEN", "TMDB_API_KEY"],
      "outputs": [".nitro/**", ".tanstack/**", ".vercel/**", ".output/**"]
    }
  }
}
