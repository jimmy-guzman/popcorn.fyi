{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "passThroughEnv": [
        "DATABASE_URL",
        "DATABASE_URL_UNPOOLED",
        "TMDB_API_TOKEN",
        "TMDB_API_KEY"
      ],
      "outputs": [".nitro/**", ".tanstack/**", ".vercel/**", ".output/**"]
    }
  }
}
